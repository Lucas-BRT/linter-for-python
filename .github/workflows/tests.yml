name: pytest
on: [pull_request]
jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
    - name: Run pytest
      uses: pavelzw/pytest-action@v2
      with:
        verbose: true
        emoji: true
        job-summary: true
        custom-arguments: '-q'
        click-to-expand: true
        report-title: 'Test Report'



